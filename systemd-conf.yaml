#sudo mkdir -p /etc/datadog-agent/conf.d/system.d
#sudo tee /etc/datadog-agent/conf.d/system.d/conf.yaml > /dev/null <<EOF
logs:
  - type: file
    path: /var/log/messages
    service: system
    source: syslog
  - type: file
    path: /var/log/syslog
    service: system
    source: syslog
    
#allow the dd-agent user to read /var/log/messages
#sudo chmod o+r /var/log/messages
#logger "Test log entry for Datadog"
